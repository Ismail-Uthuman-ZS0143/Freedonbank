django==5.2.11
djangorestframework==3.16.1
psycopg2-binary==2.9.11
python-dotenv==1.0.1
gunicorn==25.0.3

# Step 7's real content extraction (document_requests/content_extraction.py).
# pytesseract also needs the `tesseract-ocr` system package installed.
pdfplumber==0.11.9
python-docx==1.1.2
openpyxl==3.1.5
pytesseract==0.3.13
pillow==11.3.0

# Real outbound email delivery (document_requests/mail_delivery.py) -- MX
# record lookups for direct-to-MX sending, no SMTP relay/provider account.
dnspython==2.8.0
